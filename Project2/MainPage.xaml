<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Project2"
             x:Class="Project2.MainPage"
             Title="Movies">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:GenreToEmojiConverter x:Key="GenreToEmojiConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="10" Spacing="10">

            <!-- Search bar -->
            <SearchBar x:Name="SearchBarControl"
                       Placeholder="Search movies..."
                       TextChanged="SearchBar_TextChanged"/>

            <!-- Filters button -->
            <Button Text="Open Filters"
                    Clicked="OnOpenFilterClicked"
                    BackgroundColor="#6200EE"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="40"/>

            <!-- Favourites toggle -->
            <HorizontalStackLayout VerticalOptions="Center" Spacing="10">
                <Switch x:Name="ShowFavouritesSwitch"
                        Toggled="ShowFavouritesSwitch_Toggled"/>
                <Label Text="Show only favourites"
                       VerticalOptions="Center"/>
            </HorizontalStackLayout>

            <!-- Movies list -->
            <CollectionView x:Name="MoviesView"
                            SelectionMode="Single"
                            SelectionChanged="MoviesView_SelectionChanged"
                            HeightRequest="600">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="6"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Padding="8" Spacing="12" VerticalOptions="Center">
                            <Label Text="{Binding Emoji}" FontSize="28"/>
                            <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                                <Label Text="{Binding Title}" FontSize="16"/>
                                <Label Text="{Binding Director}" FontSize="12" TextColor="Gray"/>
                                <Label Text="{Binding Genre, Converter={StaticResource GenreToEmojiConverter}}" FontSize="12" TextColor="DarkGray"/>
                                <Label Text="{Binding Rating, StringFormat='Rating: {0:F1}'}" FontSize="12" TextColor="DarkGray"/>
                                <Label Text="👀 Viewed" FontSize="12" TextColor="Orange"
                                       IsVisible="{Binding IsViewed}"/>
                            </VerticalStackLayout>
                            <Button Text="♡"
                                    Clicked="OnFavouriteClicked"
                                    HorizontalOptions="End"
                                    VerticalOptions="Center"
                                    FontSize="20"
                                    Loaded="FavouriteButton_Loaded"
                                    BindingContext="{Binding}"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
